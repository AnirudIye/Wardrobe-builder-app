# BetterDresser - Handoff

_Last updated: 2026-07-16, second session (forgot-password flow added on top of the motion system → email fix → security hardening → design elevation work; all merged and pushed)._

## Goal

BetterDresser is a portfolio-grade web app for managing a digital wardrobe and getting AI styling help. Users sign up (email confirmation), build a wardrobe from photos (AI auto-tags) or web products, then get weather- and calendar-aware outfits ("Today's Recommendations", free/unlimited), gap analysis with shoppable links ("What To Buy Next", 5/day free), styling chat (DresserAI, 20/week free), AI try-on photos (TryOn, 5/week free), and account management. $5/mo Plus (Stripe) lifts all caps. A marketing landing page greets logged-out visitors.

**Stack:** FastAPI (Python 3.9, SQLite dev, Postgres-ready) + React/Vite/TS/Tailwind SPA. Text AI via provider-agnostic `services/llm.py` (Anthropic or Gemini); email via `services/email.py` (stdlib SMTP). Repo root is `Wardrobe builder app/`; remote `github.com/AnirudIye/Wardrobe-builder-app` (private).

## Current state

**`main` pushed, tree clean, in sync with origin.** Backend: **225 tests pass** (`pytest -q`). Frontend `npm run build` clean. Everything below is merged:

**2026-07-19 session 11 (avatar compression):** Profile pictures now get their own tiny, distinct treatment instead of reusing the 400px garment thumbnail. New `process_avatar(raw)` in `services/images.py`: center-crops to a square (avatars are masked into a circle, so aspect-fit letterboxing was wrong) and resizes to a fixed 96x96 (`AVATAR_SIZE`) at quality 72 (`AVATAR_QUALITY`). The upload endpoint (`routers/profile.py` `/profile/avatar`) calls it instead of `process_upload().thumbnail_bytes`. Result measured live: a 633 KB upload -> 3.1 KB served JPEG (94% smaller than the old 46 KB / 300x400 letterboxed avatar), renders crisp at the 40px settings size / 10px menu size (96px = ~2x DPR headroom). 5 new unit tests (`test_avatar_processing.py`: square, jpeg, <20 KB on noise, centered crop not squashed, rejects non-image). No schema change, no migration - purely how the bytes are processed. Existing avatar upload/remove tests still pass unchanged (they only assert avatar_url presence).

**2026-07-19 session 10 (habit pack - streak follow-ups):** User chose the habit pack from the streak follow-up menu (photo check-ins, the social pack, and Plus streak freezes remain deferred). Spec: `docs/superpowers/specs/2026-07-18-habit-pack-design.md`. (a) **Cost-per-wear**: `garments.price` (user-entered, nullable; PATCH via the existing wardrobe update; explicit null clears), `GET /fits/wear-stats` (wears counted in Python over the JSON garment_ids - same full-scan scale as streak math; per-garment cpw plus closet_value/never_worn aggregates), price input + "Worn Nx · $X/wear" line on Wardrobe cards, "Cost per wear" best-value panel on the Streak page, `wearStatsCache` cleared on every log and price change. (b) **Daily challenges**: `services/challenges.py`, 28 curated prompts keyed `date.toordinal() % len` so every user sees the same challenge on the same day (Wordle shared-reference; AI flavor deliberately rejected - per-user generation breaks the shared reference). Honor-system claim: `outfit_logs.challenge_done` via the log endpoint, +5 week points per claimed day (ceiling now 135), banner + claim checkbox on the Streak page (the Today one-tap deliberately does not claim it). Migration `b8c4d19e6f2a` (both columns), 12 new tests. Verified live: challenge banner -> claim -> "Done, +5 points" with exact points math (30+6+5=41), price $120 -> "$40.00/wear" after 3 logged wears on both Wardrobe and Streak panels. **Browser-pane gotcha:** React `onBlur` is driven by `focusout` - dispatch `FocusEvent('focusout')`, a synthetic `Event('blur')` never reaches it.

**2026-07-18 session 9 (fit streak - the competitive feature):** Wordle/Strava-style stat, spec'd with the user then built end to end (`docs/superpowers/specs/2026-07-18-fit-streak-design.md`). Daily outfit-log **streak** (headline stat, fully user-controlled) + deterministic 0-100 **closet score** (secondary; category 40 / formality 30 / seasons 20 / depth 10 over the real vision vocabulary - note it is `footwear`, not `shoes`). Comparison is **share-card only**: client-rendered 1080x1350 typographic PNG (no photos, so no s3 canvas-taint and no privacy surface) via `navigator.share(files)` with download fallback, plus a Wordle-style emoji-grid clipboard text; an anonymous percentile ("more days logged than X% of everyone logging this week") comes from one GROUP BY over the trailing 7 days. New: `outfit_logs` table (unique user+date, revision `e7a92b3f4c1d`, applied to dev DB), pure-function `services/streaks.py` (grace rule: one missed day per ISO week is auto-forgiven; unlogged today never breaks the streak; backfill allowed for yesterday only, enforced against client-supplied `today` sanity-checked to [utc-2, utc+1]) and `services/closet_score.py`, `/fits/log` + `/fits/status` (free, never quota-metered), 39 new tests. Frontend: "Streak" tab (`pages/Streak.tsx`: hero number, Mon-Sun grid, longest/points/score/percentile, garment multi-select logging), one-tap "I wore this" on Today (logs the recommendation, chip shows the streak), `components/ShareCard.tsx`, `streakCache` in store, prefetch registered. Verified live end to end (log via picker -> streak 1 -> Today chip -> backfill via API -> streak 2 -> percentile 50 with a second logger -> share PNG exports). Follow-ups deliberately out of scope (see spec): friends graph, in-app leaderboards, photo check-ins, cost-per-wear (the wear log now makes it cheap), daily challenges. **Ops note:** C: hit 0 bytes free mid-session; npm+pip caches were purged (~265 MB freed). The Recycle Bin holds ~1.1 GB and the disk needs a real cleanup pass by the user.

**2026-07-18 session 8 (signup case hole + header-pinned dialogs):** (a) The reported "pre-existing accounts can still create new ones" hole reproduced as **email case-sensitivity**: `EmailStr` keeps the local part's case and every auth lookup was an exact match, so `CaSe@x.com` registered a second account beside `case@x.com` (and a case-variant Google identity created a duplicate instead of linking). Fix: `_canonical_email` (strip+lower) in `routers/auth.py`, applied inside `_get_user_by_email` and at both account-creation sites - covers register, login, resend, forgot-password, and Google sign-in. Alembic data revision `a41f08d2c7be` lowercases pre-existing rows (refuses to run if two accounts would collapse into one address; applied to the dev DB, which was already all-lowercase). 3 new tests; verified live in the UI (case-variant register shows the 409 error note). (b) **Account-deletion confirm rendered pinned to the top of the screen** (user report): the sticky header's `backdrop-blur-md` makes the header the containing block for `fixed` descendants, and ConfirmDialog renders inside AccountMenu inside the header - so `fixed inset-0` centered it in the header strip, not the viewport. Modal (settings/customization) had the identical defect. Both now `createPortal` to `<body>`; measured centered live (card center y 360 = viewport center 360). ConfirmDialog's other call sites (Wardrobe, Calendar) are unaffected by the portal. (c) **Raw errors no longer reach users** (user report: login sometimes showed a JSON.parse error). Root cause: `api.ts` parsed every response body as JSON unguarded, so infrastructure error pages (dev proxy with the backend down, host 502s) threw SyntaxError verbatim into the UI; network failures leaked "Failed to fetch"; 422 validation arrays got JSON.stringified; and HTTP/2's empty statusText could yield a blank message. Every page renders `err.message`, so the fix is centralized in `api.ts`: `safeFetch` (network failure → status-0 ApiError with a plain sentence), `parseBody` (tolerates non-JSON), and `errorMessage` (backend `detail` string verbatim; first validation error as a sentence tagged with the field; else status-mapped copy, with 5xx always winning over contextual fallbacks). Verified live: wrong password still shows the backend's exact "Incorrect email or password", a 150-char register password shows "String should have at most 128 characters (password)", and login with the backend killed shows the friendly server message instead of a JSON.parse error.

**2026-07-18 session 7 (db audit + em-dash purge):** Query audit verdicts: no N+1 (zero ORM relationships, no queries in loops), pool already tuned (pre_ping/5+5/recycle 300), no SELECT * (ORM emits explicit columns). Fixes: optional `limit`/`offset` on GET /wardrobe/items and /calendar/events (defaults unchanged for the SPA; stable id tiebreak ordering), composite `garments(user_id, created_at)` index (revision `c9d3e51fb202`, applied to dev DB), and delete_account now projects only the two storage-key columns. Also removed every em dash from the repo (255 across 60 files): user-facing strings got real rewrites (site title now uses a colon; DresserAI fallback split into two sentences; Buy Next heuristic rationales use ", so"), comments/docs got " - ". The vendored `.claude/skills/emil-design-eng` file was deliberately left as upstream wrote it. House rule going forward: no em dashes anywhere, including comments.

**2026-07-17 session 6 (mobile + image prefetch):** (a) **Mobile horizontal scroll eliminated** - root cause was grids with only a responsive-prefixed `grid-cols-*` (e.g. `grid lg:grid-cols-3`) having no base column, so below the breakpoint they became a single implicit max-content column that overflowed (Wardrobe +7px, Calendar +17px at 320px). Added base `grid-cols-1` to every such grid (sed across pages/) + an `overflow-x-clip` safety net on the app shells (clip, not hidden, so the sticky header still works - verified computed `overflow-y: visible`). Audited at 375px AND 320px: every screen (landing, auth, all 7 tabs, dropdowns, modals) now 0 horizontal overflow. (b) **Intent-based image prefetch** (`frontend/src/prefetch.ts`): hovering/focusing a nav tab warms that tab's images before the click via `new Image()` at `fetchPriority:low`, deduped + capped, skipped under data-saver. Wired to nav `onPointerEnter`/`onFocus` in App.tsx. Only warms images from ALREADY-cached data (garment thumbs app-wide; Buy Next product thumbs after first visit) - deliberately never triggers the quota-metered Buy Next AI call on mere hover. Verified live: TryOn hover → 4 garment thumbs, Buy Next hover → 12 product thumbs, dedupe + no-image tabs → 0. **Browser-pane note:** this headless pane ignores programmatic scroll (scrollTop/scrollTo are no-ops), and React synthesizes onPointerEnter from a root `pointerover` listener (dispatch `pointerover`, not `pointerenter`, to trigger it in tests).

**2026-07-17 session 5 (deploy prep):** Repo is now one-command deployable. `render.yaml` (root) is a Render Blueprint for the backend (Python 3.12, `rootDir: backend`, `alembic upgrade head && uvicorn --proxy-headers` start command, all env vars scaffolded with secrets as `sync: false`, `JWT_SECRET` auto-generated). `frontend/vercel.json` proxies `/api/*` to the Render service (strips `/api` like the dev proxy → no CORS, `connect-src 'self'` works), and serves the CSP as a real header with `frame-ancestors 'none'` plus the same security headers the API sets. `backend/.python-version` pins 3.12.13. **Python 3.12 compatibility genuinely verified** - created a clean 3.12.13 env, installed pinned requirements (all binary wheels), full suite 162/162 pass, app imports clean under `-W error::DeprecationWarning` (only warning is upstream python-jose `utcnow()`). Production-boot smoke test confirmed the required env set (JWT_SECRET + CORS_ORIGINS; s3 vars enforced when STORAGE_BACKEND=s3). `docs/deploy.md` rewritten with concrete Neon+R2+Render+Vercel steps. Recommended stack: Neon Postgres, Cloudflare R2 media, Render backend, Vercel frontend - all free-tier; keep the self-built auth. **The one manual step:** confirm/replace the backend URL in `vercel.json` `rewrites[0].destination` (defaults to `betterdresser-api.onrender.com`, which matches the render.yaml service name).

**2026-07-17 session 4 (design skill + export + legal):** Emil Kowalski's `emil-design-eng` skill installed at `.claude/skills/emil-design-eng/` (from github.com/emilkowalski/skills) and its checklist applied: no `transition-all` anywhere (property-scoped `transition` + strong `ease-out-strong` cubic-bezier(0.23,1,0.32,1) theme easing), entrances tightened (fadeRise 320ms, stagger 300ms/40ms, tab fade 200ms), Tailwind `hoverOnlyWhenSupported` gates hover on touch. **Gotcha: Tailwind config changes need a Vite dev-server restart** (running PostCSS pipeline doesn't reload it; prod build unaffected). `GET /profile/export` ships GDPR portability (all user data as downloadable JSON, no password hash; "Export my data" in the account menu; privacy policy sect. 7 updated). ToS gained fulfillment/refund/warranty/liability sections; privacy policy rewritten for GDPR+CCPA (both earlier today).

**2026-07-17 session 3 (Google sign-in):** "Continue with Google" on the login page (login + register modes), fully built but **dormant until `GOOGLE_OAUTH_CLIENT_ID` is set in `backend/.env`** (user must create a Web-type OAuth client in Google Cloud console with JS origin `http://localhost:5173`; no secret needed, it's the GIS ID-token flow). `services/google_oauth.py` verifies the token (google-auth, already a dep via genai); `POST /auth/google` creates the account on first use (random unguessable placeholder password, so forgot-password later sets a real one; auto-links to an existing same-email account and marks it verified since Google vouches for the address; rejects Google-unverified emails). `GET /auth/google/config` tells the SPA whether to render the button; prod CSP meta gained Google's documented `accounts.google.com/gsi/` entries. Verified: 8 endpoint tests, dormant state live (no script load, no UI). The Google popup itself is untestable without a real client id.

**2026-07-17 session 2 (onboarding + fixes + indexes):** (a) **Location picker fixed** - typing "Waterloo" used to silently save Waterloo, Iowa; now `GET /profile/location/search` lists all candidates (`weather.geocode_candidates`) and the widget offers a pick list, `POST /profile/location` accepts explicit lat/lon. (b) **Calendar fixed** - the date input was clipped invisible in the narrow lg rail (needs ~110px content width; now stacks on lg) and the header shows today's date. (c) **ToS checkbox** required on register. (d) **Buy Next price filter** (client-side chips: Any/25/50/100/200). (e) **Wardrobe camera capture** (`components/CameraCapture.tsx`, reusable; TryOn still uses its own inline flow - could adopt the component later). (f) **First-run style survey** (`components/StyleSurvey.tsx`, overlay when profile.style_preferences is null; save/skip both persist via existing `PATCH /profile`; prefs already flowed into buy-next/outfit prompts server-side, verified live: budget answer visibly shaped picks). (g) **Composite DB indexes** for quota counting and today's-events (`b7c21d40aa11` Alembic revision; dev wardrobe.db stamped at baseline then upgraded - it was never stamped before). Browser-pane gotcha reconfirmed: synthetic ref-clicks sometimes don't land on this hidden page - drive clicks via injected JS before concluding a button is broken. Follow-up from a user screenshot: the weather widget itself was broken in the rail (viewport-keyed `sm:flex-row` + fixed `w-64` map crushed the text column to a few px) - it now always stacks, map full-width below the text, geometry verified via injected measurements.

**2026-07-17 session 1:** login hardening - the distinct login error messages tried earlier in the day were **reverted the same day** (user's call after a security checklist review): login is back to one generic "Incorrect email or password" for both failure halves, and a **per-account lockout** was added (`core/lockout.py`, `LOGIN_LOCKOUT_ATTEMPTS=8` failures in `LOGIN_LOCKOUT_MINUTES=15` → 429; success or a redeemed password reset clears it; shares `RATE_LIMIT_ENABLED`). Skeleton loaders added for the app boot shell, the TryOn render wait, and the DresserAI thinking bubble. **An NVIDIA API key was investigated for TryOn and stored in `backend/.env` (`NVIDIA_API_KEY`, git-ignored): it cannot power TryOn** - the hosted FLUX Kontext NIM is preview-locked to 3 example images (`Expected: example_id` on any real image; Kontext pro/max 404), and text-to-image models can't preserve the user's face. Google billing (item 4 below) remains the real unblock; the NVIDIA key does offer free hosted text LLMs if a third text provider is ever wanted.

0. **Forgot-password flow** (spec: `docs/superpowers/specs/2026-07-16-forgot-password-design.md`): login page gains "Forgot password?" → email form → generic check-your-email card; the emailed `/?reset_token=` link opens a set-a-new-password view that signs the user straight in. Token is a stateless JWT pinned to a fingerprint of the current password hash, so it is single-use with no DB migration; redeeming it also marks the email verified. `POST /auth/forgot-password` (5/hr, anti-enumeration) + `POST /auth/reset-password` (400 on any bad/dead token). Verified live end-to-end (reset, single-use rejection, dead-token UI path).

1. **Landing motion system** - seamless `Marquee` (measures + duplicates, provably no seam), `useReveal` scroll reveals, `CountUp`, `HeroField` canvas particles, `SplitText` headline, cursor parallax. All reduced-motion-gated and fail-open.
2. **Email confirmation WORKS end-to-end** (register → email → click → verified → login, verified live incl. DB flip). Transport: **Gmail SMTP app password** on the dedicated `BetterDresserConfirmation@gmail.com` account (values in git-ignored `backend/.env`). Brevo was tried and abandoned: its free tier accepts SMTP sends then rejects internally ("sender not valid"), and Gmail throttles its shared pool.
3. **Quota:** buy-next is **5/day** (`quota.enforce(..., days=1)`, `BillingStatus.remaining_today`/`daily_limit`); chat/tryon stay weekly. Tabs renamed: My Wardrobe / Today's Recommendations / What To Buy Next.
4. **Security hardening + prod data layer** (see `docs/deploy.md` + CLAUDE.md "Security posture"): `ENVIRONMENT=production` fail-fast posture, security-headers middleware, per-IP auth rate limiting, SSRF-guarded image fetches (TryOn reads owned garments via `storage.read`), build-time CSP meta, 401 token-expiry interceptor, **Alembic wired** (baseline + email_verified grandfathering), **psycopg3** Postgres support, **S3/R2 storage backend** (config-selected, local default).
5. **Design elevation (de-slop redesign)** - research-driven (sources in `docs/superpowers/` plan + this session's commits): Figtree body font paired with Ramaraja, favicon/meta/OG, `::selection`/`:focus-visible`/scrollbar detail layer; landing rebuilt without AI-tells (no badge-over-H1, no stat banner, no numbered steps, no identical card grids - editorial alternating rows, connected flow, de-twinned pricing, concrete copy); app pages all relaid on a `PageHeader`/`EmptyState` system (Wardrobe rail layout, Today look card, DresserAI chat surface with starter prompts, Buy Next editorial rows, TryOn two-panel, Calendar two-pane with formality tints, Upgrade live allowance meters, split-panel Login). Palette/Ramaraja/all motion kept.
6. TryOn fixes: camera video-ref race fixed (preview works); Buy Next picks loadable from TryOn (explicit button, quota-aware); 503 copy is "TryOn isn't available right now".

## Next steps (in rough priority)

1. **Getting ready for deployment.** The repo side is done: `render.yaml`, `frontend/vercel.json`, Python 3.12 pin, and `docs/deploy.md` (concrete Neon → R2 → Render → Vercel order plus the cross-reference backfills: CORS_ORIGINS/FRONTEND_BASE_URL, Google OAuth origin, Stripe webhook). What remains is account work only: create Neon Postgres, Cloudflare R2 bucket, Render Blueprint, Vercel project; fill the `sync: false` env vars; confirm the backend URL in the `vercel.json` rewrite. Also rotate the secrets that passed through chat: Gmail app password, NVIDIA key, and the old Brevo keys (revoke in the Brevo dashboard).

Still open from earlier sessions, lower priority:

- **Swap the landing showcase mock for real screenshots** - marked `TODO(D10)` in `frontend/src/pages/Landing.tsx`; capture the real tabs into `frontend/public/screens/`.
- **TryOn generation** stays blocked until billing is enabled on the Google account (~$0.03-0.04/image, zero code changes); the NVIDIA key cannot power it (hosted Kontext is example-locked).

## Known issues / environment gotchas

- **Harness quirks seen today:** the in-app browser pane cannot paint (hidden page) - verify geometry via injected JS or the user's real browser; transient "classifier unavailable" outages block Bash/MCP for minutes (file Read/Write/Edit keep working - keep editing, batch the builds/commits for when it recovers).
- Windows/Anaconda: prepend Anaconda dirs to PATH before venv python or `_ssl` fails (see below). A transient "paging file too small" DLL error can hit python one-shots; retry.
- Pre-existing noise: benign passlib/bcrypt `__about__` traceback in logs; Python 3.9 EOL warnings (an upgrade is a worthwhile future task).
- Dev servers are session-bound background tasks - they die with the session; restart per below.
- `backend/.env` holds real secrets (JWT, API keys, Gmail SMTP) - git-ignored, never commit it.

## Running it locally

Two terminals (both must run together):

```bash
# Backend (from backend/)
export PATH="/c/Users/<you>/anaconda3:/c/Users/<you>/anaconda3/Library/bin:/c/Users/<you>/anaconda3/Scripts:$PATH"
.venv/Scripts/python -m uvicorn app.main:app --reload   # :8000, docs at /docs

# Frontend (from frontend/)
npm run dev                                              # :5173, proxies /api -> :8000
```

Open http://localhost:5173. Dev DB (`backend/wardrobe.db`) has 3 verified accounts. With SMTP configured in `.env` (it is), new signups require a real email confirmation; blank the `SMTP_*` values to revert to auto-verify for throwaway testing. Tests: `pytest -q` from `backend/` (127; conftest blanks all keys + disables the rate limiter). See `CLAUDE.md` for architecture, the quota/security patterns, and conventions.
